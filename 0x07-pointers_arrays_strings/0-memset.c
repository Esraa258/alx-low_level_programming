#include "main.h"

/**
 * _memset - fills memory with a constant byte.
 * @p: pointer to a memory area
 * @b: value that is to fill up memory area @p
 * @n: max bytes to use
 * Return: pointer p
*/

char *_memset(char *p, char b, unsigned int n)
{
	unsigned int x;

	for (x = 0; x > 0; a++, x++)
	{
		*(p + x) = b;
	}

	return (p);
}
